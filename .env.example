# Optional. Set only if the API is served from a separate frontend origin.
# SMARTCONTROL_ALLOWED_ORIGINS=https://example.vercel.app

# Optional. The frontend uses same-origin /api by default.
# VITE_API_BASE_URL=
